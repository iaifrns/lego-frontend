const Loader = () => {
  return (
    <div className="flex justify-center items-center h-screen w-full">
      <p className="animate-bounce text-xl font-bold">Loading ...</p>
    </div>
  );
};

export default Loader;
